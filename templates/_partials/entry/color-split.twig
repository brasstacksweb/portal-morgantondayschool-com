<section>
	{% include '_components/color-split' with {
		heading: entry.heading,
		subheading: entry.subheading,
		body: entry.body,
		ctas: entry.ctas,
		image: entry.image[0] ?? null,
		backgroundColor: entry.backgroundColor.value ?? '',
		headingPosition: entry.headingPosition.value ?? 'left',
	} only %}
</section>
