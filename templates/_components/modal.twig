{##
 # Modal component
 #}

{# Variables #}
{% set handle = 'modal' %}

{# Markup #}
{% import '_icons' as icons %}

<div class="{{ handle }}" data-comp-name="{{ handle }}">
	<dialog>
		<button type="button" aria-label="Close">
			{{ icons.x() }}
		</button>
		<section></section>
	</dialog>
</div>
